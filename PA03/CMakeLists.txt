cmake_minimum_required(VERSION 2.8)
SET(CMAKE_CXX_FLAGS "-std=c++11")
project( Bayesian )
find_package( OpenCV REQUIRED )
add_executable( Bayesian main.cpp )
target_link_libraries( Bayesian ${OpenCV_LIBS} )
